One note: if you want the SIOME5 parent-child hierarchy to remain completely untouched, comment out this block in _update_metadata_from_db():

if db_parent_node_id:
    variable.set("ParentNodeId", str(db_parent_node_id))
	
	
Your views.py can remain the same:

xml_data = generate_siome5_xml_from_db_nodes(
    nodes=nodes,
    template_path=str(template_path),
)

This will give:

<?xml version="1.0" encoding="utf-8"?>
<UANodeSet ... LastModified="current-date-time">

and it will avoid adding new <Value> tags where the SIOME template did not have them.



Ver3:
our views.py usage remains:

from .siome5_exporter import generate_siome5_xml_from_db_nodes

This version will:

Keep SIOME5 structure
Update LastModified
Use double quotes in XML declaration
Update only simple existing values
Skip EUInformation / ExtensionObject / LocalizedText
Not create new <Value> tags



Ver4

Update views.py

Change your export call to pass the server endpoint:

xml_data = generate_siome5_xml_from_db_nodes(
    nodes=nodes,
    template_path=str(template_path),
    endpoint_url=server.endpoint,
)

So export now works even if the DB has only imported tags but no stored readings.

Why temperature was zero:

Server Temperature is changing
but DB latest reading was not available
so exporter used fallback 0

Now it will try live OPC UA read using the DB node’s node_id.